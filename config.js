/*
TODO: write a json config that hosts details about each command name. if a command is both
      in the commands folder and the JSON it gets edited, other wise the default is set by
      just having the command name and other fields should be blank

TODO: Handle all of the set ups - should be able to catch all of the errors.
      


example for creating the bot design.
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
  
  toString() {
    return this.age + " is " + this.age + " years old";
  }
}

var people = new Array();
// object properties are being assigned here
people["john"] = new Person("John", 22);
people.sabila = new Person("Sabila", 20);

for (key in people) {
  console.log(people[key]);
}
*/
let fs = require('fs');

class OtterBot {
  constructor() {

  }

  
}


